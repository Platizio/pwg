/**
 * Route shim. The page lives in the revamp workspace beside the components and
 * styles it owns; src/pages stays the route manifest React Router reads.
 */
export { default } from '../../Platizio_Global_Revamp/pages/Terminal'
