# P8 Finish Implementation Plan

> **For agentic workers:** this phase is run by the controller, not by implementer subagents: it captures, reviews, fixes and documents the whole site.

**Goal:** Close the rebuild the way the impeccable run demands: inspect every rebuilt route at desktop and mobile, run the design detector, put the evidence in front of a fresh finish reviewer, act on its disposition, record the design system from the built world, and update the two product documents.

**Ground rules:** identical to P0's. No git writes: Aayush decides when to commit.

---

### Task 1: Captures

- [ ] Start the dev server (`preview_start` name `platizio`). For each route in `/`, `/pricing`, `/about`, `/media`, `/articles`, `/articles/topic/taxation`, `/articles/lrs-explained`, `/faqs`, `/help`, `/user-guide`, `/terms`, `/privacy`, `/disclaimer`, `/does-not-exist`: open at 1440 wide with entrance motion settled (scroll to the bottom once, then to the top), capture a full-page screenshot into `.impeccable/review/desktop-<slug>.png`; repeat at 390 wide into `.impeccable/review/mobile-<slug>.png`. If the Browser pane cannot composite (hidden pane), front it with `tabs_select` first; a blank or half-loaded capture is recaptured, never sent on.
- [ ] Open every capture once and confirm it shows what its name claims.
- [ ] Record, per route: console errors (`read_console_messages`), horizontal overflow (`document.documentElement.scrollWidth === window.innerWidth`), and the LCP element's visibility before hydration (`curl -s http://localhost:3000/<route> | grep -c 'opacity:0'` must print `0`).

### Task 2: The detector and the mechanical fixes

- [ ] Run `node /Users/aayushsharma/.claude/skills/impeccable/scripts/detect.mjs --json` over `components/marketing` and `app/(site)/styles`. Fix what is mechanical (a duplicate declaration, a literal colour, a missing focus state); carry the rest to the reviewer.
- [ ] Run `npm test && npx tsc --noEmit && npm run lint && npm run build`. All clean.

### Task 3: The finish review

- [ ] Spawn the shipped `impeccable-finish-reviewer` agent, fresh (no conversation history), with: the original request (Aayush's brief, quoted), the confirmed answers (serif display + sans body superseded by v4's faces; night moments; copy preserved; industrial and clean; v4 components and the products page's cards; the home section order), the artifact path (`app/(site)` and `components/marketing`), every screenshot path from Task 1 named required, the direction contract (`.impeccable/surfaces/app-site-page-tsx.md`), the detector findings, the two decision mockups as critique references (`scratchpad/platizio-clean-home.html`, noting it was the approved direction's mock), the craft-floor reference path `/Users/aayushsharma/.claude/skills/impeccable/reference/craft-floor.md`, and the line "code-led build; no approved comp".
- [ ] Verify the return carries the five contract sections. Act on the disposition word: `recapture` → recapture and re-review; `rebuild` → execute and re-review; `fix` → apply the material fixes in one batch, recapture the same viewports, send back for the verdict; `ship` → continue. Two fix rounds is the budget; if the second verdict still lists open items, put the table in front of Aayush.

### Task 4: The record

- [ ] Spawn the shipped `impeccable-documenter` with the project root, the artifact path, the direction contract, `PRODUCT.md`, the reference path `/Users/aayushsharma/.claude/skills/impeccable/reference/document.md`, and the boundary `app/(site)` + `components/marketing`. It writes `DESIGN.md` and its sidecar from the built world.
- [ ] Read `DESIGN.md` once: it must describe what shipped (the tokens in `tokens.css`, the five glass surfaces, the night-moment rule, the section rhythm), not the plan.
- [ ] Update the `site-revamp-scope` memory with the outcome and the list of routes rebuilt.

### Task 5: Handoff

- [ ] Report to Aayush: the reviewer's disposition at its scope, the routes verified, the tests and build state, the files that changed, and what is left uncommitted. Offer the commit as his decision.
